import { StyleSheet, Text, View } from 'react-native';

const ChatBox = () => {
	return (
		<View>
			<Text>index</Text>
		</View>
	);
};

export default ChatBox;

const styles = StyleSheet.create({});
